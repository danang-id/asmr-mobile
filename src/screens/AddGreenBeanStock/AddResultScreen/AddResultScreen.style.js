import {StyleSheet} from 'react-native';

const AddResultScreenStyle = StyleSheet.create({
	container: {
		minHeight: '100%',
	},
	scrollView: {},
	scrollViewContentContainer: {},
});

export default AddResultScreenStyle;
