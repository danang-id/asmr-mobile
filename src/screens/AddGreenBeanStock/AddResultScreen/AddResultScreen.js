import React, {FC} from 'react';
import {SafeAreaView} from 'react-native';
import {Text} from '@ui-kitten/components';

const AddResultScreen: FC = () => {
	return (
		<SafeAreaView>
			<Text>Add Result Screen</Text>
		</SafeAreaView>
	);
};

export default AddResultScreen;
